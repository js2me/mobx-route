---
"mobx-route": major
---

First release. Removed deprecations.

Removed the deprecated `loadView` API and all props that existed only to support it.

Dropped the `react-simple-loadable` dependency; it was only used by `loadView`.